# =============================================================================
# Meshwork-AutoDev Environment Configuration
# Copy to .env and fill in values: cp .env.example .env
# =============================================================================

# ── Required ─────────────────────────────────────────────────────────────────
RUNNER_SECRET=your-runner-secret-min-16-chars
DASHBOARD_PASSWORD=your-dashboard-password

# ── Database (bundled by default; switch to external by editing these) ───────
POSTGRES_MODE=bundled
RUNNER_DB_HOST=postgres
RUNNER_DB_PORT=5432
RUNNER_DB_NAME=runner
RUNNER_DB_USER=runner
# generated by setup.sh — do not use a literal default in production
RUNNER_DB_PASSWORD=
# For external Postgres: set POSTGRES_MODE=external and point RUNNER_DB_HOST
# at your DB host. Skip the bundled container by NOT passing --profile bundled-db.

# ── Jira Cloud (OPTIONAL — leave blank for built-in issue tracker) ───────────
# JIRA_DOMAIN=https://yourorg.atlassian.net
# JIRA_EMAIL=you@example.com
# JIRA_API_TOKEN=your-jira-api-token

# ── Telegram (OPTIONAL — leave blank for in-app notifications only) ──────────
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token
# TELEGRAM_CHAT_NOTIFICATIONS=your-chat-id
# TELEGRAM_CHAT_ENGINEERING=your-chat-id
# TELEGRAM_CHAT_MARKETING=your-chat-id
# TELEGRAM_CHAT_SALES=your-chat-id

# ── N8N Workflow Automation (OPTIONAL — core runner works without it) ────────
# N8N_BASIC_AUTH_USER=admin
# generated by setup.sh — N8N editor basic-auth password
N8N_BASIC_AUTH_PASSWORD=
# generated by setup.sh — password for N8N's Postgres database
N8N_DB_PASSWORD=

# ── External Webhook Access (OPTIONAL) ───────────────────────────────────────
# NGROK_AUTHTOKEN=your-ngrok-authtoken
# NGROK_DOMAIN=your-domain.ngrok.app

# ── Outgoing Notification Webhook (OPTIONAL — Slack/Discord/Teams/custom) ────
# NOTIFICATION_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ── Project Directory (mounted into runner container) ────────────────────────
# PROJECT_DIR=/path/to/your/project

# ── Retention & Internal URLs ────────────────────────────────────────────────
# Days to keep finished jobs/conversations/read notifications; 0 disables pruning
RETENTION_DAYS=0
# dashboard→runner base URL; compose sets http://runner:3210
RUNNER_URL=http://localhost:3210

# ── API Keys (OPTIONAL) ─────────────────────────────────────────────────────
# ANTHROPIC_API_KEY=your-anthropic-key  (only if using API mode instead of OAuth)
# ATTIO_API_KEY=your-attio-api-key      (for sales agents)
# ZAI_API_KEY=your-zai-api-key          (for creative-assets agent)
# SNYK_TOKEN=your-snyk-token            (for security scanning via Snyk MCP)

# ── Security & Resilience Hardening ──────────────────────────────────────────
# Host interface the runner/dashboard/n8n ports bind to. Loopback by default;
# set to 0.0.0.0 ONLY if you intentionally expose the stack on your network
# (put a TLS-terminating reverse proxy in front — see SECURITY.md).
# BIND_ADDRESS=127.0.0.1
# Reject new jobs with 429 once this many are queued (0 disables)
# MAX_QUEUE_DEPTH=200
# Hard timeout for runner outbound HTTP (callbacks, alerts), milliseconds
# OUTBOUND_HTTP_TIMEOUT_MS=30000
# Extra hosts agents may reach with curl/wget (comma-separated suffixes).
# Package registries, GitHub, Atlassian and the local stack are always allowed.
# MESHWORK_EGRESS_ALLOWLIST=hooks.slack.com,api.example.com
# Set to 0 to disable the egress filter entirely
# MESHWORK_EGRESS_ENFORCE=1
# Dashboard login rate limit (attempts per window per IP)
# LOGIN_MAX_ATTEMPTS=5
# LOGIN_WINDOW_MS=60000
# Backups kept per database by scripts/backup-postgres.sh
# BACKUP_RETENTION_COUNT=14

# ── Webhook Verification ─────────────────────────────────────────────────────
# Shared token external callers must present (?token=... or x-webhook-token
# header). Generated by setup.sh. Add it to your Jira webhook URLs:
#   https://<your-n8n>/webhook/jira-in-progress?token=<WEBHOOK_SHARED_TOKEN>
WEBHOOK_SHARED_TOKEN=
# Set to true AFTER configuring tokens on external callers. When false the
# "Verify caller" nodes log but do not block (safe migration default).
WEBHOOK_VERIFICATION_ENFORCE=false
